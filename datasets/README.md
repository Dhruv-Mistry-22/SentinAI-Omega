# Datasets

Place training / validation datasets here.

Recommended structure:

```
datasets/
├── real/          # Authentic camera photographs
├── ai_generated/  # AI-generated images (DALL-E, Midjourney, SD, etc.)
└── deepfakes/     # Face-swapped / manipulated images
```

> Large dataset folders should be added to `.gitignore`.
